/**
 * Andamio de la SPA. El layout de verdad —sidebar, topbar con selector de
 * empresa/sucursal, router y guardas de ruta— es F1-040.
 */
export default function App() {
  return (
    <main className="app">
      <h1>Monitor SoftRestaurant</h1>
      <p>
        Carril <code>/web</code> en pie. El panel se construye a partir de F1-040.
      </p>
    </main>
  );
}
