<Project Sdk="Microsoft.NET.Sdk.Worker">

  <!--
    Esqueleto del agente. Lo que de verdad hace (Serilog con rotacion, config en
    C:\ProgramData\ArkonAgente\config.json, `sc create`, publish single-file
    self-contained x64, el comando `agente test`) es F1-020.

    TargetFramework, Nullable, ImplicitUsings y LangVersion vienen de
    ../../Directory.Build.props: no se repiten aqui.
  -->

  <PropertyGroup>
    <RootNamespace>ArkonAgente</RootNamespace>
    <AssemblyName>agente</AssemblyName>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.Hosting" Version="8.0.1" />
  </ItemGroup>

</Project>
